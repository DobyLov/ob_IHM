export class Roles {

    public idRoles: number;
    public roles: string;

    // constructor (idRoles: number, roles: string) {
    //     this.idRoles = idRoles;
    //     this.roles = roles;
    // }
}